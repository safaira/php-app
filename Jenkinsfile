pipeline{
  agent any
  stages{
    agent
    label: slave-node1
     stage('Install Puppet agent on slavenode') {
       steps {
         // clone git repository
         git url:  
       }
     }
       
  }
}
